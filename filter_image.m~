function [img] = filter_image(img, frame_size, filter_size)

#Applying a high pass filter, since it is used for edge detection
high_pass = [0.25,0.5,0.25;0.5,5,0.5;0.25,0.5,0.25];
img = imfilter(img, high_pass);

end
